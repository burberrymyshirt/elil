
; TODO: this should obviously fail

(deffn foo (i:int) (
    (echo "i is: " i "\n")
    (if (lt i 10)
    (
        (let a 20)
        (foo a)
    )
    ; should be undefined on second call.
    (echo a "\n"))
))

(foo 5)
