Current and pressing bugs:
[] examples/full.elil doesn't print numbers as string. It prints the literal char 'E',
   as that is the ascii charater of the digit 69. Types should probably be passed from
   the parser to the evaluator, instead of just passing lit. But I think its because
   the add function returns charlist containing a number, which gets printed as the ascii literal.
   We have just been using strings for everything else so far, so it needs to be redone I think.

Todos and wants

--- Most pressing
[x] dqstring parsing and a string literal. Don't know if I want just one type of string, which does everything though.
[x] adding more functions. "add" is the only one missing from the examples.
 - [] add actual argument validation to the add function.
 - [] type juggling to print whatever values are there. Possibly some format print.

[x] let bindings.
 - @see global_namespace issue below
[] conditional operators & boolean operators. "if", "and", "or", "not" etc.
[] Define functions.
  - [] Lambdas first.
  - [] Closures to capture scope.

[] Adding meta to the parser, so we can error report properly.
[] Just better errors in general for parsing errors and undefined stuff or whatever.
   E.g. if parsing fails with an unknown node, it should not fall into a "** (CaseClauseError) no case clause matching:" error
 - [] redo the lexer, as the do_lex() function does not know about the rest of the lexer state.
      I would kinda like a central function that gets the state, but doesn't send it to the
      rest of the do_lex() calls, and then let those calls return something like {:error, msg}
      when they fail. That way we don't have to provide the lexer state all over the call stack to report errors.
      It just happens in a single place, and can therefore easily be replaced with a different logger.
      @see logging errors

 - [] Evaluator should also error out when invalid function arguments are given. Right now it just assumes the correct params are given.
    - [] Right now some functions are just variadic in nature and allat, so make some validation
         that determines whether a function is variadic or not. Or if it should even be allowed to be variadic at all.

[] Recursion, so we can actually start building something useful.
[] Build a program that actually does stuff, so we can figure out what is still missing.
[] Add meta data from lexer to parser, in order to report arity/variadic parameters.
   Right now we just ignore parameters when there are more than the function needs.
   This would also help with error logging in general, as we can provide line numbers and such.
   @see logging errors

[] Integer parsing from string should support more than just base 10
[] Make the parser more strict on syntax, so we can fail before reaching the evaluator in more cases.
   No work should really be done if we can detect something wrong before trying to run.
   Should be quite simple in many cases, with more matching of expected tokens in each parse function.
 - [x] Handle parenthesis properly, as this example below will still run correctly, even though it has an extra :cparen at the end:
      @see improper_syntax
      ```elil
      (let asdf "i am asdf")                 ; this let binding should be able to take an :ident or maybe even a keyword
      (echo "value of asdf: \"" asdf "\"\n")) ; this should also be able to print whatever the let binding introduced above
      ```

[] Implement a debugging system, as right now it is kind of difficult to debug the lexer and parser an allat

--- Later on down the line
[] with statements when nested cases are used. They just seem kind of confusing, so not touching for now.
   nested cases are very readable and work fine, but are just pretty bloated.
[] multi-file programs through require.
[] building a standalone executable.
[] maybe implement multi stage compilation. Right now everything is single pass, which makes undefined variables annoying.
   We can essentially know at parse time when something is undefined, so we don't run any code before everything is in scope.
[] introduce namespaces. Everything is in a global namespace for now, but idk if that is very nice, especially since everything is parsed in a single pass.
   Doesn't necessarily have anything to do with namespaces being nice, but whatever. Dual pass could make global issues easier to debug I guess.
   Things also don't go out of context once out of scope. This is the biggest issue as of right now, as you won't be able to reuse names ever.
   @see global_namespace

[] I would very much like some kind of plugin system for this language, so my own DSLs can be implemented on top of this.
   That means creating some sort of Behaviour interface and then "link" with it from the Elil interpreter and extend the interpreter via that.
   And by interpreter, I mean having different Behaviours for the lexer, parser and evaluator to hook into.
   We could introduce specific keywords with specific rules and maybe even operators or something.
   idk how useful it will be, but I think it would be a kind of sick idea to play with.

[] Go through all TODOs in the code :D

--- ideas for projects using Elil
- Postman collection json parser, that transforms to raw curl calls in structured directories, so I don't have to use that garbage anymore.
- Issue tracker that is independent of git remotes, maybe even hosted in-repo as part of the source code, like tsoding/tatr does.
- Management tooling of my personal home-lab. This is less of a specific idea, and more a grouping of ideas, as I don't really wanna depend on the sea of choices out there. I wanna own it myself. Although this language might not be suitable for that, as it depends on the erlang vm...
- Personal finance cli program with persistence of some sort, because fuck Excel.
